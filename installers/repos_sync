#!/usr/bin/env zsh

mkdir -p "${HOME}/src"

git clone https://github.com/kassio/repos_sync.git "${HOME}/src/repos_syncer"

cd ${HOME}/src/repos_syncer;

mise install &&
  source "${HOME}/.zshrc" >/dev/null 2>&1 &&
  ruby -v

gem update &&
  bin/setup &&
  bin/repos_sync
