#!/usr/bin/env bash

title="Notifying"
message=""

while true; do
  case "$1" in
    -t | --title)
      shift
      title=$1
      shift
      ;;
    * )
      message="$*"
      break
      ;;
  esac
done


osascript -e "display notification \"${message}\" with title \"${title}\""
