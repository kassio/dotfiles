#!/usr/bin/env bash

URL="https://iterm2.com/shell_integration/install_shell_integration_and_utilities.sh"

curl -L "${URL}" | bash

mv "${HOME}/.iterm2_shell_integration.zsh" "${HOME}/.dotfiles/lib/iterm2_shell_integration.zsh"
