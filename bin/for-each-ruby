#!/usr/bin/env bash

OIFS=$IFS
IFS='\n'
versions=($(rbenv versions --bare))
IFS=$OIFS
versions+=("system")

for rb in ${versions[@]}
do
  export RBENV_VERSION=${rb}
  echo "with ruby: $(rbenv version-name)"
  eval "$@"
done

unset RBENV_VERSION
