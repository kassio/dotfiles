#!/usr/bin/env bash

function title {
  local bullet="▸ "

  case $1 in
    -c|--color)
      shift
      local color=$1
      shift
      ;;
    *)
      local color="74"
      ;;
  esac

  local msg="$@"

  printf "$(print_c -c 3 ${bullet}) $(print_c -b -c ${color} $*)\n"
}

title $@
