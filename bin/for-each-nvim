#!/usr/bin/env zsh

cmd="<c-c>:$1<cr>"
while read server; do
  echo ">> executing '${cmd}' in ${server}"
  /opt/homebrew/bin/nvim --server "${server}" --remote-send "${cmd}"
done < <(find "${HOME}/.cache/nvim" -name "*.pipe")
