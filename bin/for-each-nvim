#!/usr/bin/env zsh

cmd="$1"
while read -r server; do
  echo ">> executing '${cmd}' in ${server}" >> /tmp/for-each-nvim.log
  /opt/homebrew/bin/nvim --server "${server}" --remote-send "${cmd}"
done < <(find "${XDG_CACHE_HOME}/nvim" -name "*.pipe")
