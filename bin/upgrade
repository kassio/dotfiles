#!/usr/bin/env bash

brew update

pcks=( ack bash bash-completion brew-cask ctags ctags-fishman git heroku-toolbelt highlight markdown mercurial python rename terminal-notifier the_silver_searcher tmux tree watch wget zsh )
for pck in "${pcks[@]}"
do
  brew upgrade $pck 2>/dev/null
done

brew doctor
