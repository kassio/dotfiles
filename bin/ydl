#!/usr/bin/env bash

URL="$1"
shift
DIR="${HOME}/Movies"

case ${URL} in
  *list*)
    youtube-dl -o "${DIR}/%(playlist_title)s/%(playlist_index)s %(title)s.%(id)s.%(ext)s" ${URL} $@
    ;;
  *)
    youtube-dl -o "${DIR}/%(title)s.%(id)s.%(ext)s" ${URL} $@
    ;;
esac
