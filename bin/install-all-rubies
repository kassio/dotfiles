#!/usr/bin/env bash

if [ "$1" != "" ]
then
  echo "Usage: "
  echo "install-all-rubies path"
  exit -1
fi

rubies=$(cat $1/.ruby-version | sort -u)

for version in $rubies
do
  rbenv install $version
done
