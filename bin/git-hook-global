#!/usr/bin/env bash

if [[ -x "bin/spring" ]]
then
  echo "stopping spring"
  (bin/spring stop &>/dev/null) &
fi

echo "retagging"
(ripper-tags -R &>/dev/null || rm -f tags && ripper-tags -R &>/dev/null) &
