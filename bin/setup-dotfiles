#!/usr/bin/env zsh

export DOTFILES="${HOME}/.dotfiles"
export PATH="${PATH}:${DOTFILES}/bin"

"${DOTFILES}/bin/installers/setup-macos"
"${DOTFILES}/bin/installers/stow"
"${DOTFILES}/bin/installers/upgrade"
