#!/usr/bin/env bash

pushd "${PWD}" > /dev/null && trap 'popd &> /dev/null' EXIT HUP INT QUIT TERM

IFS=:
for repo in ${REPOS[@]}
do
  pushd ${repo} &>/dev/null

  if [ -d ".git" ]
  then
    eval "$@"
  fi

  popd &>/dev/null
done
