#!/usr/bin/env bash

if [ "$1" = "--help" ]; then
  echo "Upgrade zsh_plugins"
  exit
fi

title "updating zsh plugins"
antibody bundle < "${HOME}/.dotfiles/lib/zsh_plugins" > "${HOME}/.dotfiles/home/zsh_plugins.sh"
antibody update
