#!/usr/bin/env zsh

if [ "$1" = "--help" ]; then
  echo "update nvim plugins"
  exit
fi

title "[nvim] updating neovim plugin"
echo > ~/.cache/nvim/packer.nvim.log
nvim -c 'Upgrade!'
cat ~/.cache/nvim/packer.nvim.log
