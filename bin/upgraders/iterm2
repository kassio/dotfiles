#!/usr/bin/env zsh

if [ "$1" = "--help" ]; then
  echo "update iterm2 dependencies"
  exit
fi

title "[nvim] updating shell integration"

curl -L https://iterm2.com/shell_integration/install_shell_integration_and_utilities.sh |
  bash
