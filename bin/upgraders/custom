#!/usr/bin/env zsh

title "[custom] upgrade your custom components (those that aint in github 🤫)"
if [ "${1}" = "-h" ] || [ "${1}" = "--help" ]; then
  exit
fi

if type custom-upgrade &>/dev/null; then
  custom-upgrade
fi
