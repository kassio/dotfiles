#!/usr/bin/env zsh

if [ "$1" = "--help" ]; then
  echo "Install/Upgrade go packages"
  exit
fi

title "Install/Upgrade golang packages"
while read -r pkg; do
  go install "${pkg}"
done < "${HOME}/.dotfiles/lib/gopkgs"
