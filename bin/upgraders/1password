#!/usr/bin/env zsh

if [ "${1}" = "-h" ] || [ "${1}" = "--help" ]; then
  echo "[1password] update 1password completion"
  exit
fi

title "[1password] updating 1password completions"
mkdir -p "${ZSH_CONFIG_HOME}/completions"
op completion zsh > "${ZSH_CONFIG_HOME}/completions/1password.zsh"
