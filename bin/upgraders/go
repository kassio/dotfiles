#!/usr/bin/env zsh

if [ "$1" = "--help" ]; then
  echo "Install golang packages"
  exit
fi

title "[gopkgs] golang packages"
while read -r pkg; do
  subtitle "installing \"$(echo "${pkg}" | sed 's#.*\/\(.*\)@.*#\1#')\""
  go install "${pkg}"
done < "${XDG_CONFIG_HOME}/go/default_packages"
