#!/usr/bin/env zsh

if [ "$1" = "--help" ]; then
  echo "update golang packages"
  exit
fi

title "[golang] updating golang packages"
cat "${ASDF_GOLANG_DEFAULT_PACKAGES_FILE}" |
  xargs -P 4 -Ipkg go install pkg
