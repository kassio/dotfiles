#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/inline"

gemfile do
  source "https://rubygems.org"

  gem "gitlab"
  gem "irb"
end

def gitlab
  @gitlab ||= Gitlab.client(
    private_token: ENV.fetch("GITLAB_PRODUCTION_TOKEN", nil),
    per_page: 100
  )
end

IRB.start
