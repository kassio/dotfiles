#!/usr/bin/env zsh

local delta="${HOMEBREW_PREFIX}/bin/delta"
local delta_theme="gruvbox-light"

if [ "$(macos-appearance)" = "dark" ]; then
  delta_theme="gruvbox-dark"
fi

macos-appearance

"${delta}" --syntax-theme="${delta_theme}" "$@"
