#!/usr/bin/env zsh

local delta="${HOMEBREW_PREFIX}/bin/delta"
local delta_theme="gruvbox-dark"

if [ "$(cat -s "${HOME}/.cache/iterm_profile")" = "light" ]; then
  delta_theme="gruvbox-light"
fi

"${delta}" --syntax-theme="${delta_theme}" "$@"
