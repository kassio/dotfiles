#!/usr/bin/env bash

_retag() {
  ctags || (rm tags && ctags)
  ctags-gems || (rm tags.gems && ctags-gems)
}

case $1 in
  -v|--verbose)
    _retag
    ;;
  *)
    exec &>/dev/null
    _retag
    ;;
esac
