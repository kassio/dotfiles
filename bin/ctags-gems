#!/usr/bin/env ruby
# frozen_string_literal: true

require 'bundler'

system('rm -rf tags.gems &>/dev/null')

gems_paths = Bundler.load.specs.map(&:full_gem_path).join(' ')
system("ripper-tags -R --append=no --languages=ruby --tag-relative=yes -f tags.gems #{gems_paths} 2>/dev/null")
