#!/usr/bin/env ruby

require 'bundler'

system('rm -rf tags.gems &>/dev/null')

gems_paths = Bundler.load.specs.map(&:full_gem_path)
system("ctags -R --append=no --languages=ruby --tag-relative=yes -f tags.gems #{gems_paths.join(' ')} 2>/dev/null")
