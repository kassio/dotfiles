#!/usr/bin/env bash

ctrlp_cache_file="$HOME/.nvim_data/ctrlp/${PWD//\//%}.txt"
if [[ -e $ctrlp_cache_file ]]
then
  rm 2>/dev/null &
fi

if [[ -x "bin/spring" ]]
then
  bin/spring stop &>/dev/null &
fi

(ctags || rm -f tags && ctags) &
