#!/usr/bin/env zsh

local bat="${HOMEBREW_PREFIX}/bin/bat"
local bat_theme_name="gruvbox-light"

if [ "$(macos-appearance)" = "dark" ]; then
  bat_theme_name="gruvbox-dark"
fi

BAT_THEME="${bat_theme_name}" "${bat}" "$@"
