#!/usr/bin/env zsh

local bat="${HOMEBREW_PREFIX}/bin/bat"
local bat_theme="tokyonight_day"

if [ "$(macos-appearance)" = "dark" ]; then
  bat_theme="tokyonight_night"
fi

BAT_THEME="${bat_theme}" "${bat}" "$@"
