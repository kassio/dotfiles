#!/usr/bin/env zsh

if [ "$1" = "--help" ]; then
  echo "Upgrade zsh_plugins"
  exit
fi

title "[antibody] updating zsh plugins"
antibody bundle < "${ZDOTDIR}/antibody_list" > "${ZDOTDIR}/plugins"
antibody update
